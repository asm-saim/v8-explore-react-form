## 1. How to get input from form:
- e.target.name.value-> e.target(target the total form container).name(specific input fields name attribute).value
## 2. use form action to get value:
form action={handleForm} , handleForm(formData) console.log(fromData.get("input fieds name data"))